version: '3'
services:
  node-app:
   build:
    context: .
    args:
     NODE_ENV: develpment
   volumes:
    - ./:/app
    - /app/node_modules
   environment: 
    - NODE_ENV=develpment
    - SESSION_SECRET=secret
    - DATABASE=mongodb://1223d:<password>@mongo:27017/?authSource=admin
    - DB_PASSWORD=1223d


   command: npm run dev
  
  